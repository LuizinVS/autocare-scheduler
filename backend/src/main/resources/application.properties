spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/autocare_scheduler
spring.datasource.username=${DB_USER:luiz}
spring.datasource.password=${DB_PASSWORD:admin}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.servlet.context-path=/api

app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:4200}
app.jwt.secret=${JWT_SECRET:autocare-dev-jwt-secret-change-me-before-production}
app.jwt.expiration-ms=${JWT_EXPIRATION:86400000}
app.admin.email=${ADMIN_EMAIL:luiz.mariath3@gmail.com}
app.admin.password=${ADMIN_PASSWORD:Trocar@123}
